{% extends 'TopxiaWebBundle::bootstrap-modal-layout.html.twig' %}

{% set modal_class = 'modal-lg' %}

{% block title %}添加+作业{% endblock %}
{% block body %}

<form id="course-material-form" class="form-horizontal" action="{{ path('homework_add', {courseId:course.id, lessonId:lesson.id}) }}" enctype="multipart/form-data" method="post">
  <div class="form-group">
    <div class="col-md-10 col-md-offset-1  controls">
      
      {% if not lesson %}
        <span class="control-text text-warning">暂无资料，请上传。</span>
      {% endif %}
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-10 col-md-offset-1 controls" id="material-file-chooser">
      {% include 'HomeworkBundle:Homework:file-chooser.html.twig' %}
      <input type="hidden" name="fileId">
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-10 col-md-offset-1 controls">
      <textarea class="form-control" name="content" rows="2" placeholder="填写资料简介 (可选)"></textarea>
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-10 col-md-offset-1 controls">
      <button class="btn btn-primary" id="material-upload-btn" type="submit">添加</button>
    </div>
  </div>

  <input type="hidden" name="_csrf_token" value="{{ csrf_token('site') }}">

</form>

<script>app.load('course-manage/material-modal')</script>

{% endblock %}

{% set hide_footer = true %}